import React from 'react';
import {Text, View} from 'react-native';
import { TextLocal } from './styledComponents';

const TextCustom = () => {
  return (
    <View>
      <TextLocal>HELLO FROM TEXT INPUT</TextLocal>
    </View>
  );
};

export default TextCustom;
