import React from 'react';
import { View} from 'react-native';
import { CustomText } from './styledComponents';

const Layout = () => {
  return (
    <View>
      <CustomText>Layout</CustomText>
    </View>
  );
};

export default Layout;
